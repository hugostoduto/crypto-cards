import React from 'react';
import { Loader } from './Loader';

export default {
  title: 'Loader',
  component: Loader,
};

export const Template = () => {
  return (
    <div>
      <Loader />
    </div>
  );
};
